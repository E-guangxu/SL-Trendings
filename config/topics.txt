# 每日推送要跟的话题。一行一个，# 开头是注释，空行忽略。
# 一行里也可以用逗号写多个话题（例：AI, 游戏）。
#
# ⚠️ 语义是「添加」，不是「替换」：每天 08:05 那次推送仍然是
#     全站榜（Reddit r/all + YouTube 全球热门）
#   再加上下面每一个话题各一段：
#     最新新闻（Google News）+ Reddit 讨论 + YouTube 最新视频
#
# 想多跟一个话题 → 往下加一行；想停掉某个 → 给那行加回 #。
# 全部注释掉 = 只推全站榜。
#
# 想让某个话题只在临时查询时用、不进每日推送，就别写在这里，
# 改用手动触发 / node trigger-daily-hot.mjs --topics "xxx"。

# ---- 当前生效 ----
科技

# ---- 备选，想用时去掉行首的 # ----
# AI
# 游戏
# 美股
